name: Verify LLM Build Artifacts

on:
  pull_request:
    branches: [main]
    paths:
      - 'docs/**'
      - 'i18n/**'
      - 'plugins/**'
      - 'lib/**'
      - 'data/**'
      - 'middleware.js'
      - 'scripts/audit-llms-coverage.mjs'
      - 'scripts/fix-llms-urls.mjs'
      - 'scripts/verify-llms-build.mjs'
      - 'scripts/verify-middleware-paths.mjs'
      - 'scripts/verify-evm-compatibility-summary.mjs'
      - 'scripts/sync-evm-compatibility-summary.mjs'
      - 'docusaurus.config.js'
      - 'vercel.json'
      - 'static/cheatsheet.md'
      - 'src/pages/cheatsheet.js'
      - 'src/components/Cheatsheet/**'

jobs:
  verify-llms:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Verify middleware path resolution
        run: yarn verify-middleware-paths

      - name: Verify EVM compatibility summary
        run: yarn verify-evm-compatibility

      - name: Build site
        run: yarn build
        env:
          MENDABLE_KEY: ${{ secrets.MENDABLE_KEY }}
          COOKBOOK_PUBLIC_API_KEY: ${{ secrets.COOKBOOK_PUBLIC_API_KEY }}
          GTM_CONTAINER_ID: ${{ secrets.GTM_CONTAINER_ID }}

      - name: Verify LLM and markdown artifacts
        run: yarn verify-llms-build

      - name: Audit llms.txt coverage and size
        run: yarn audit-llms-coverage
